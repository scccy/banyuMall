<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.origin.banyu.wechatWork.mapper.WechatWorkMomentsMapper">

    <!-- 根据moment_id查询动态 -->
    <select id="selectByMomentId" resultType="com.origin.banyu.wechatWork.entity.WechatWorkMoments">
        SELECT * FROM wechatwork_moments
        <where>
            moment_id = #{momentId}
            AND deleted = 0
        </where>
    </select>

    <!-- 根据用户和时间范围统计动态数量 -->
    <select id="countByUserAndTimeRange" resultType="long">
        SELECT COUNT(*) FROM wechatwork_moments
        <where>
            wechatwork_users_id = #{wechatworkUsersId}
            AND deleted = 0
            <if test="startTime != null">
                AND created_time &gt;= FROM_UNIXTIME(#{startTime}/1000)
            </if>
            <if test="endTime != null">
                AND created_time &lt;= FROM_UNIXTIME(#{endTime}/1000)
            </if>
        </where>
    </select>

    <!-- 根据用户和时间范围查询热门动态 -->
    <select id="selectHotMomentsByUserAndTimeRange" resultType="com.origin.banyu.wechatWork.entity.WechatWorkMoments">
        SELECT * FROM wechatwork_moments
        <where>
            wechatwork_users_id = #{wechatworkUsersId}
            AND deleted = 0
            <if test="startTime != null">
                AND created_time &gt;= FROM_UNIXTIME(#{startTime}/1000)
            </if>
            <if test="endTime != null">
                AND created_time &lt;= FROM_UNIXTIME(#{endTime}/1000)
            </if>
        </where>
        ORDER BY created_time DESC
        LIMIT #{limit}
    </select>

</mapper> 