<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.origin.banyu.wechatWork.mapper.WechatWorkUserMapper">

    <!-- 根据wechatwork_user_id查询用户 -->
    <select id="selectByWechatworkUserId" resultType="com.origin.banyu.wechatWork.entity.WechatWorkUser">
        SELECT 
            wechatwork_user_id,
            name,
            mobile,
            department,
            position,
            gender,
            email,
            avatar,
            status,
            enable,
            isleader,
            created_time,
            updated_time,
            created_by,
            updated_by,
            deleted
        FROM wechatwork_users 
        WHERE wechatwork_user_id = #{wechatworkUserId}
        AND deleted = 0
    </select>
    
    <!-- 根据wechatwork_user_id更新用户 -->
    <update id="updateByWechatworkUserId" parameterType="com.origin.banyu.wechatWork.entity.WechatWorkUser">
        UPDATE wechatwork_users 
        SET 
            name = #{name},
            mobile = #{mobile},
            department = #{department},
            position = #{position},
            gender = #{gender},
            email = #{email},
            avatar = #{avatar},
            status = #{status},
            enable = #{enable},
            isleader = #{isleader},
            updated_time = #{updatedTime},
            updated_by = #{updatedBy}
        WHERE wechatwork_user_id = #{wechatworkUserId}
        AND deleted = 0
    </update>

</mapper> 